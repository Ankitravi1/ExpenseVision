VITE_API_URL=http://localhost:5000/api

# Optional: Google sign-in button (must match backend GOOGLE_CLIENT_ID)
# VITE_GOOGLE_CLIENT_ID=

# Optional: web push (must match backend VAPID_PUBLIC_KEY)
# VITE_VAPID_PUBLIC_KEY=
